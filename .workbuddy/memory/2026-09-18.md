# 2026-09-18

## 用真实长路线（Far 02 GroupA）验证 GPX→路书链路

用户给了第二条真实 GPX：`C:\Users\surface\Downloads\Far+02+GroupA.gpx`
（Strava 路线 `routes/3526826341795948174`，作者 Lure Xu #Rapha）

### 实测数据（这条路线）
- 距离 **150.58 km**，海拔 49~1126 m
- 爬升：原始 +2728 m，平滑 w=9 后 +2507 m
- 轨迹点 2834，**无时间戳**，**有 4 个航点（wpt）**
- 面板路线：4 段爬坡（30.2km/18.3km/+962m/5.2%/5★、76.2/3.2/4.0%/2★、
  80.4/1.7/7.9%/2★、124.7/11.5/5.5%/5★），转弯候选 53 个

### 🔴 重大发现：这条 GPX 自带补给点航点
4 个航点全部精确落在轨迹上（偏差 0/0/0/4 m），这是**骑手人工标的真实补给点**：

| 里程 | 名称 | sym |
|---|---|---|
| 28.4 km | 固定补给点 | Generic |
| 48.7 km | 补给点1 | Rest Area |
| 82.1 km | 补给点2 | Rest Area |
| 137.0 km | 补给点3 | Rest Area |

**这证实了我 09-11 的一条推断**（当时对覆卮山 GPX 我说"Strava 这类导出的 GPX
可能自带提示点，但实测那条没有"）。现在有了反例：**同样是 Strava 导出的 GPX，
这条就有航点**。结论修正为：**航点有或没有取决于路线作者标没标，必须每次检查、
不能假设**。

### 代码改动
1. `tools/gpx_to_roadbook.py`
   - `parse_gpx()` 现在提取 wpt（lat/lon/name/sym），放进 `meta["waypoints"]`
   - 新增 `project_waypoints()`：把航点投影到轨迹算里程（阈值 300m，
     超出的判定"不在路线上"并丢弃+报告）
   - `build()`：**补给点优先用 GPX 航点，没有才用 place_fuel() 规则估算**
     - 规则估算的点落在爬坡区间 → 直接丢弃
     - **真实航点不能丢**（骑手亲标的位置）→ 顺延到爬坡结束后 0.4km，并报告
   - 新增 `--no-wpt` 开关（强制用规则估算）
   - **修复 HALFWAY 倒挂 bug**（见下）
2. `tools/roadbook.py`：爬坡第二行左列**省略 " km" 单位**（见下）
3. `02_Roadbook/02_Roadbook.ino`：同步省略单位

### 🐛 修掉一个真 bug：HALFWAY 倒挂
**现象**：第 2 页出现 `76.2 CLM 3.2 / 79.4 ★★` 之后跟一行 `77.7 HALFWAY`
—— 77.7 比 79.4 小，读者会以为写错。

**根因**：原有"避开拥挤"逻辑是"从真中点一路 +0.4 往后找"，它只在开头检查过了
`_in_climb()`，中途会**跨过爬坡起点钻进爬坡区间内部**。爬坡占两行、第二行显示的
是"结束公里数"，所以区间内的事件必然倒挂。

**修法**：改成**候选扫描** —— 真中点 ±8km 每 0.4km 撒候选，过滤掉
①爬坡区间 ②与邻近事件距离 <1.2km，然后**取离真中点最近的那个**。
本案例：77.7 → **74.9**（移出 76.2~79.4 区间），偏离真中点仅 0.4km。

⚠️ **这个 bug 覆卮山那条恰好没触发**（那条的中点没落进爬坡区间），
所以是新路线才暴露出来的。以后加新路线都要看一眼有没有"数字倒挂"。

### 爬坡第二行拥挤：省略 " km" 单位
三位数公里数时放不下：`136.2 km`(72px) + 间距(4) + `5.5%`(40) + 5星(65) = **181px**，
而可用宽度只有 188 - 8(缩进) = **180px**。差 1px，导致 `136.2 km` 和 `5.5%` 粘连。

试过的方案与结论：
- ❌ 缩小星星：13px 已是可辨认下限（12px 尖角就糊，11px 更差），省不了
- ❌ 坡度整数（5.5%→5%）：丢精度
- ❌ 减缩进：视觉上"从属感"会消失
- ✅ **第二行省略 " km"**：省 20px，**不损失任何数字精度**（小数点照留、坡度照留一位小数）

### 密度问题：150km 路线默认 14 个事件太少
默认 `--max-events 14` 被 4 段爬坡（占 8 行）+ 4 个补给点吃掉，
**只剩 4 个转弯提示**（150km 山路）。用 `--max-events 18` 后 → 8 个转弯、4 页。

**待用户确认**：是否把"事件上限按距离自适应"做成默认（如 `距离/8` 夹在 12~26）。
