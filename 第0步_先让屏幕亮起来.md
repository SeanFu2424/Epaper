# 第 0 步：让屏幕显示 "42.6 km"

> 目标：从零开始，让屏幕上出现一行字。**不碰中文、不碰路书、不碰 SD 卡。**  
> 资料页：<https://www.waveshare.net/wiki/ESP32-S3-ePaper-1.54>

先说清楚一件事：**"往开发板写数据"有两个层次**

| 层次    | 是什么                                     | 这一步做不做   |
| ----- | --------------------------------------- | -------- |
| ① 烧程序 | 把 `.ino` 代码编译后通过 USB 写进芯片，**代码里带着要画什么** | ✅ 现在就做这个 |
| ② 传内容 | 运行时把图片/文字传进去（SD 卡 / 蓝牙 / Wi-Fi）         | ❌ 以后再说   |

你现在要的是 ①。搞懂 ①，② 就只是换个数据来源而已。

---

## 0. V1 vs V2 区别（30 秒看完）

> **你已经在 V2 上**，下面是背景知识，万一你拿到的是 V1 或者要刷别人的程序：

|              | V1               | V2（你的）                   |
| ------------ | ---------------- | ------------------------ |
| 芯片           | ESP32-S3FH4R2    | **ESP32-S3-PICO-1-N8R8** |
| Flash        | 4MB              | 8MB                      |
| PSRAM        | 2MB QSPI         | 8MB OPI                  |
| 屏幕引脚         | 一样               | 一样                       |
| Arduino 板卡参数 | **PSRAM 选 QSPI** | **PSRAM 选 OPI**          |

**唯一一个会踩坑的地方**：刷了 V1 的示例到 V2 上，编译能过但运行会卡在 PSRAM 分配失败。  
反过来 V2 示例跑在 V1 上同理。所以**下板子之前一定先确认版本**。

---

## 1. 装 Arduino IDE

下载 [Arduino IDE 2.x](https://www.arduino.cc/en/software)（选 2.3.x），一路下一步装完。

> ⚠️ 国内下载板卡包基本都会超时，**装不上是正常的**——直接跳到第 2 节末尾的「装不上？」看解法。

---

## 2. 加 ESP32 板卡支持

打开 Arduino IDE：

1. **文件 → 首选项 → 附加开发板管理器网址**，填：
   ```
   https://espressif.github.io/arduino-esp32/package_esp32_index.json
   ```
2. **工具 → 开发板 → 开发板管理器** → 搜 `esp32` → 装 **esp32 by Espressif Systems**
3. 版本选 **3.3.11**（最新）

---

### 装不上？报 `Download failed` / `13 INTERNAL`

先看报错的**结尾**，两种错误的成因完全不同：

| 报错结尾                                                                                        | 真实含义                                                    | 看哪节                |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------ |
| `wsarecv: A connection attempt failed`  
`context deadline exceeded`                        | TCP 层就超时，压根没连上 GitHub                                   | ① / ② / ③          |
| `stream error: stream ID N; PROTOCOL_ERROR`                                                 | **已经连上了**，大文件传到一半被掐断                                    | ② / ④              |
| `read tcp 127.0.0.1:xxxx -> 127.0.0.1:443`  
`aborted by the software in your host machine` | 你机器上的本地代理（Watt Toolkit / Steam++ 之类）掐的，**不是** GitHub 的锅 | ② `-NoProxy` 或先关代理 |

第二种其实是进展——说明 GitHub 下得动，只是 **HTTP/2 的流被中间设备重置**了  
（代理、公司防火墙、杀毒软件的 HTTPS 扫描都会干这事）。

<omitted>

#### ① 有代理 → 最快，一劳永逸

**文件 → 首选项 → 网络 → 手动代理**，填你的代理地址（Clash 默认 `127.0.0.1:7890`，看你软件里的实际端口）。

设完重启 IDE 再装，1.8 GB 自己下完。**推荐这个**——下一步装 GxEPD2 库也要从 GitHub 下，有代理后面全顺。

没代理也不想折腾节点？装个 **Watt Toolkit**（原 Steam++，微软商店就有）或 **FastGitHub**，  
里面有一键 GitHub 加速，开了再点安装通常就能下动，比自己配代理省事。

#### ② 没代理 → 跑脚本下（带断点续传）

工作区里有现成脚本，右键 `download_esp32.ps1` → **使用 PowerShell 运行**：

```powershell
cd C:\Users\surface\WorkBuddy\2026-09-07-15-27-20\_环境
.\download_esp32.ps1          # 先下 ESP32-S3 必需的 659 MB，验证网络通不通
.\download_esp32.ps1 -All     # 能下动就补齐全部 1803 MB
.\download_esp32.ps1 -All -NoProxy   # 上面失败时再试这行，绕开本地代理
```

文件会下到 `C:\Users\surface\AppData\Local\Arduino15\staging\packages\`  
（Arduino 自己的缓存目录）。**中断了再跑一次会接着下**，每个文件下完自动校验 SHA256。

全部下完后回 Arduino IDE 重新点安装，它直接用缓存，不再联网。

> **本地代理认识一下**：脚本会优先走 `$env:HTTPS_PROXY`。  
> 你这台机器上 `HTTPS_PROXY=http://127.0.0.1:52817`，那是 **Steam++ / Watt Toolkit**（装在 `C:\Program Files\Steam++\`）的系统代理。  
> 它跟你直连都能下，但中间有些 HTTPS 流会被它的 MITM 自己掐掉——刚才报错结尾的 `127.0.0.1:443` 就是它的环回端口。  
> **遇到这类错就加 `-NoProxy`**，让脚本绕过它直连 GitHub，反而最稳。

> **关于 RISC-V 等大件**：1.8 GB 里 716 MB 是 RISC-V 工具链（给 ESP32-C3/C6/P4 用的），427 MB 是其他芯片的预编译库。  
> 你这颗 ESP32-S3 是 Xtensa 内核，**全部用不到**。  
> 但板卡管理器没有"只装一部分"的选项，想装上就得全下。脚本默认先下 S3 必需的 659 MB，是为了先确认你的网络能不能下动 GitHub，省得盲等 1.8 GB 才发现不通。

#### ③ 用迅雷 / IDM 手动下

脚本下得慢的话，把报错里的 URL 粘进迅雷——迅雷对 GitHub 资源有缓存加速，通常能跑满带宽。  
下完把文件按**原名**放进上面那个 `staging\packages` 目录，再回 IDE 点安装。

#### ④ 关掉 HTTP/2，让 IDE 自己下（专治 PROTOCOL_ERROR）

Arduino IDE 的后端是 Go 写的，它用 HTTP/2 跟 GitHub 通信，长连接容易被中间设备重置。  
`_环境\start_arduino_no_http2.bat` 会用 `GODEBUG=http2client=0` 启动 IDE，强制走 HTTP/1.1：

```
先完全退出 Arduino IDE（托盘里也要退），再双击这个 bat
```

然后再点一次安装。HTTP/1.1 没有多路复用的流，不会被 `PROTOCOL_ERROR` 打断。

> 这个方案适合想让 IDE 自己搞定全部流程的人。  
> 如果还不行，回到 ② 用脚本下——脚本里的 curl 已经写死 `--http1.1`，走的是同一条规避路径。

**已经下好的部分不会丢**：不管是 IDE 还是脚本，已完成的文件都留在 `staging\packages` 里，  
下次继续时直接跳过。你随时可以跑 `.\download_esp32.ps1` 看进度。

---

## 3. 装库

**工具 → 管理库**（或左侧书本图标），搜 `GxEPD2` → 安装。

弹窗问要不要装依赖（Adafruit GFX 等）→ 选 **Install all**。

> ⚠️ 别装名字像的 `GxEPD` 或 `Epd`，API 不兼容会编译报错。

---

## 4. 板卡参数设置

USB 线插上板子（**必须是能传数据的线，不是纯充电线**），然后：

| 设置项                 | 选什么                                     |
| ------------------- | --------------------------------------- |
| Board               | **ESP32S3 Dev Module**                  |
| Port                | 出现的 COM 口（没出现就是线的问题或没装驱动）               |
| **USB CDC On Boot** | **Enabled** ← 不开串口一个字都看不到               |
| **PSRAM**           | **OPI PSRAM** ← 都选这个，V1/V2 一样           |
| **Flash Size**      | V1 选 **4MB (32Mb)**；V2 选 **8MB (64Mb)** |
| Flash Mode          | QIO 80MHz                               |
| Partition Scheme    | 默认即可                                    |
| CPU Frequency       | 240MHz (WiFi)                           |

---

## 5. 烧程序

1. 打开 `00_HelloScreen/00_HelloScreen.ino`
2. 点 **上传**（右上角 → 箭头）
3. 等编译 + 上传完成

> 这块板子用的是 ESP32-S3 内置 USB CDC，**自动下载电路会替你按 BOOT**，所以正常情况下你**完全不用按**。
>
> **只有上传卡在 `Connecting...` 一直不动**（板子没进下载模式）才需要手动介入：
> 按住 **BOOT 键** → 看到 IDE 出现上传进度条 → 松开。
> 这种兜底操作**不是必做流程**，是给异常情况留的退路。

> 板子原生 USB CDC 在 Win10+ / macOS / Linux 一般免驱。如果设备管理器里 COM 口带黄色感叹号，先装一下 [Silicon Labs CP210x](https://www.silabs.com/developers/usb-to-uart-bridge-vcp-drivers) 或 [Espressif 官方驱动](https://www.espressif.com/en/support/download/other-tools)。



---

## 6. 看结果

- 屏幕会闪几下，然后出现 **`42.6 km`**
- 打开 **工具 → 串口监视器**，波特率 **115200**，应该看到：


  ```
  boot
  refreshed
  done
  ```
  中间还夹着 GxEPD2 打印的一堆屏幕诊断信息（有就说明驱动通信成功）

**画面会一直留着，拔掉 USB 也不掉**——这就是墨水屏的双稳态特性。

---

## 6.5 电池模式：长按电源键开关机（**固件自锁**，不是 IP5306）

先纠正一个之前写错的地方：这块板**没有 IP5306**，也没有硬件电源开关。
电源键是一套**固件自锁电路**，官方 `user_config.h` 和例程 `07_BATT_PWR_Test` 写得很明确：

| GPIO | 官方信号名 | 方向 | 作用 |
|---|---|---|---|
| **GPIO18** | `BAT_KEY` | 输入 | 读 PWR 按键，**按下 = 低电平** |
| **GPIO17** | `BAT_Control` | 输出 | **电池供电自锁：高 = 维持供电，低 = 断电** |

### 电池模式为什么会白屏 —— 真正的原因

```
按住 PWR 键 → 硬件给板子通电（只在手指按着时成立）
    ↓
ESP32 启动，跑 setup()
    ↓
固件从来没碰过 GPIO17 ← 问题在这里
    ↓
手指松开 → 没有任何东西维持供电 → 电源被切断
    ↓
屏幕停在 SSD1681 上电复位后的全白状态
    ↓
你看到：白屏
```

**插 USB 时为什么不白屏**：USB 的 VBUS 直接给板子供电，根本不走 GPIO17 这条自锁链路。
这也解释了为什么"只要插 USB 就正常"。

### 正确做法：setup() 第一行就把电源接管过来

```cpp
void setup() {
  pinMode(17, OUTPUT);
  digitalWrite(17, HIGH);   // ← 必须第一行！电池供电自锁

  // ... 后面才是屏幕初始化
}
```

官方 `board_power_bsp.cpp` 就是这么写的：

```cpp
void VBAT_POWER_ON()  { gpio_set_level(17, 1); }   // 维持供电
void VBAT_POWER_OFF() { gpio_set_level(17, 0); }   // 断电（= 关机）
```

### 开关机的正确操作

| 操作 | 会发生什么 |
|---|---|
| **开机**：按住 PWR 键 | 板子通电启动，固件在 <0.5 秒内把 GPIO17 拉高，接管供电 |
| **松开 PWR 键** | 板子继续运行（因为固件已经接管了） |
| **关机**：按住 PWR 键 1.5 秒 | 固件显示 "OFF" → 把 GPIO17 拉低 → 真正断电 |
| **短按** | 无响应（固件没处理短按） |

> ⚠️ 开机时**要按住约 1 秒再松手**——要等固件跑到 `digitalWrite(17, HIGH)`。
> 按一下就松，可能固件还没接管，直接断电。
>
> ⚠️ 这块板**没有 RST 键**。要硬复位只能拔 USB 重插。

### 别再用 delay 了

之前的 `delay(2000)` / deep sleep 那套是针对"IP5306 软启动"的假设，**那个假设是错的**，已经全部删除。
现在不需要任何 delay——官方例程也是通电后立刻 init 屏幕。

### 推荐使用方式

- **插 USB 开发**：随便调试，不受电源键影响
- **电池使用**：按住 PWR 约 1 秒松手 → 屏幕出现 `42.6 km`
- **关机**：按住 PWR 约 1.5 秒 → 屏幕显示 `OFF` → 断电
- **重启**：关机再开机，或拔插 USB

---

## 7. 不亮？按这个顺序查

| 现象                               | 原因                   | 处理                                         |
| -------------------------------- | -------------------- | ------------------------------------------ |
| 串口完全没输出                          | USB CDC 没开 / 数据线不带数据 | 回到第 4 步；换根手机能传文件的线                         |
| 上传失败 `Failed to connect`         | 板子没进下载模式（少见，自动下载一般没问题）| 兜底：按住 **BOOT** 键 → 点上传 → 看到进度条动 → 松开 BOOT |
| 串口有输出但屏幕全白                       | **最常见**：屏幕电源没开       | 代码里 `digitalWrite(EPD_PWR, LOW)` 必须有，见下面说明 |
| **插 USB 正常，一用电池就白屏**             | **电池供电自锁 GPIO17 没拉高**（第 6.5 节） | `pinMode(17,OUTPUT); digitalWrite(17,HIGH);` 必须是 setup() 第一行 |
| 编译报 `GxEPD2_154_GDEY0154D67` 未定义 | 库版本太老或面板不同           | 装最新 GxEPD2；或把类名换成 `GxEPD2_154_D67` 再试      |
| 屏幕花屏/有黑边                         | 供电不足或 SPI 太快         | 换 USB 口；把 `4000000` 改成 `2000000` 降速        |
| 有淡淡残影                            | 正常，多刷几次会淡            | 不用管，后面做路书时会定时全刷                            |

### 关于屏幕电源（这块板子最容易踩的坑）

屏幕有独立的电源开关，**GPIO6，低电平才通电**：

```cpp
pinMode(EPD_PWR, OUTPUT);
digitalWrite(EPD_PWR, LOW);    // 开屏电
// ... 刷新 ...
digitalWrite(EPD_PWR, HIGH);   // 关屏电，省电
```

GxEPD2 **不会**帮你管这个脚，必须自己写。漏了就是「串口一切正常，屏幕死白」。

---

## 8. 硬件速查（官方资料确认，板载直连不用接线）

| 信号       | GPIO     | 说明                         |
| -------- | -------- | -------------------------- |
| EPD_PWR  | 6        | 屏幕电源，**LOW=开**             |
| EPD_BUSY | 8        | 刷新忙信号                      |
| EPD_RST  | 9        | 复位                         |
| EPD_DC   | 10       | 命令/数据                      |
| EPD_CS   | 11       | SPI 片选                     |
| EPD_SCLK | 12       | SPI 时钟                     |
| EPD_MOSI | 13       | SPI 数据                     |
| **VBAT_PWR** | **17** | **电池供电自锁，HIGH=保持供电，LOW=断电关机** |
| **PWR 键**  | **18** | **电源按键，按下=LOW**            |
| BOOT 键   | 0        | 上电按住 = 下载模式                |
| BAT_ADC  | 4        | 电池电压采样（VBAT = 读数×2）         |
| SD 卡     | 39/40/41 | CLK/MISO/MOSI（TF 卡要 FAT32） |

屏幕面板：**GDEY0154D67**，200×200 黑白，驱动 IC **SSD1681**，支持局部刷新。

> 注意：屏幕用的是 GPIO12/13，而 ESP32-S3 的默认 SPI MOSI 是 GPIO11、默认 SS 是 GPIO10。  
> 所以**必须手动 `SPI.begin(12, -1, 13, -1)` 重映射**，直接 `display.init()` 是不行的。

---

## 下一步

屏幕亮了之后，我们再决定显示什么内容、怎么把内容送进去。  
之前做的路书渲染工具链在 `_archive/` 里，等这一步通了再拿出来接上。
