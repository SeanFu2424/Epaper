# 墨水屏骑行路书 — 新电脑恢复指南

> 项目已上 GitHub，**换电脑的主路径就是 `git clone`**。
> 仓库：https://github.com/SeanFu2424/Epaper （私有）
>
> 本文档最后更新：2026-09-11（项目进度 V1.4）

---

## 0. 最快路径：三条命令

在新电脑上装好 Git（或 GitHub Desktop）后：

```bash
cd D:\            # 或你想放项目的任意目录
git clone https://github.com/SeanFu2424/Epaper.git
cd Epaper
```

私有库首次会弹 GitHub 登录窗口，用你的账号（**SeanFu2424**）登一次即可，之后不用再登。

**如果你不想用命令行**：装 **GitHub Desktop** → `File → Clone repository` → 选 `Epaper` → `Clone`。之后所有操作都是点按钮。

> ⚠️ 代码只占恢复工作的一小半，**剩下的在下面第 3 节**（Arduino 环境）。

---

## 1. 项目现在到哪一步了

| 阶段 | 状态 |
|---|---|
| 第 0 步：点屏 + 电池开关机 | ✅ 完成（`00_HelloScreen/`，里程碑，别改坏） |
| 第 1 步：长文本自动分页 + 按键翻页 | ✅ 完成（`01_Paging/`） |
| 第 2 步：路书事件清单 | ✅ **V1.4 当前活跃**（`02_Roadbook/`），等烧录验证页脚状态栏 |
| V2：SD 卡读 JSON（换路线不重编译） | 待启动 |
| GPX → JSON 分析器 | 待启动 |
| 接 intervals.icu 数据 | 待启动（最终目标） |

**下一次开机的第一件事**：看 `第2步_路书事件清单.md` 的「V1.4 待验证」清单，
烧一次 `02_Roadbook.ino`，确认页脚时间/电量是否正常显示。

---

## 2. 哪些东西跟账号走、哪些要自己搬

| 东西 | 在哪儿 | 换电脑后 | 怎么办 |
|---|---|---|---|
| **源代码 / 文档 / 工具** | GitHub `Epaper` 仓库 | ✅ 已在云端 | `git clone` |
| **项目记忆**（决策史/踩坑记录） | `.workbuddy/memory/` | ✅ **也已在仓库里** | clone 下来就有 |
| 云端 Profile、历史对话检索 | WorkBuddy 服务端 | ✅ 自动同步 | 不用管 |
| 用户级偏好 | `~/.workbuddy/MEMORY.md` | ❌ 不同步 | 见下面「怎么搬」 |
| ESP32 板卡包（1.8 GB） | `%LOCALAPPDATA%\Arduino15\` | ❌ 不同步 | 重装（第 3 节），或拷 `staging\packages\` |
| Arduino 库 | `Documents\Arduino\libraries\` | ❌ 不同步 | 重装（第 3 节） |
| Steam++ 的 GitHub 加速 | 系统级 | ❌ 不同步 | 重装 Steam++ |

### 搬项目记忆到新电脑（重要）

`git clone` 出来的 `.workbuddy/memory/` 是**旧项目的记忆**。但 WorkBuddy 在新电脑上
会为会话新建一个**不同名字的目录**（形如 `WorkBuddy\2026-XX-XX-XX-XX-XX\`），
不会自动读到你 clone 下来的那份。

**做法**：在新电脑的 WorkBuddy 里新建一个会话后，把 clone 下来的
`.workbuddy/memory/` 整个文件夹，复制到新会话目录的同名位置：

```
Epaper\.workbuddy\memory\   →   WorkBuddy\<新会话名>\.workbuddy\memory\
```

**更省事的做法**：直接在新电脑上用 clone 下来的这个文件夹当项目目录
（在 WorkBuddy 里「打开已有文件夹」选它），记忆就在原地，不用复制。

### 用户级偏好怎么搬

`~/.workbuddy/MEMORY.md`（新电脑上是 `C:\Users\<用户名>\.workbuddy\MEMORY.md`）
里存的是跨项目习惯，比如「输出要分事实/实测/推断三档」「你没有用过 GitHub」。
**内容不多，跟 AI 说一句「请把这些记到用户级记忆里」再贴给它就行**，
或者直接把文件拷过去。

---

## 3. 新电脑装 Arduino 环境（照抄即可）

### 3.1 需要装什么（版本是原电脑实测的，照这个装最稳）

| 组件 | 版本 | 怎么装 |
|---|---|---|
| Arduino IDE | 2.x | 官网下载安装 |
| **esp32 板卡包** | **3.3.11** | 见 3.2 |
| **GxEPD2** | **1.6.9** | 库管理器搜 `GxEPD2` |
| **Adafruit GFX Library** | **1.12.6** | 库管理器搜 `Adafruit GFX` |
| **Adafruit BusIO** | **1.17.4** | 通常随 GFX 一起装 |

只有 esp32 板卡包难装（1.8 GB，走 GitHub），库都是几 MB，正常能装。

### 3.2 装 esp32 板卡包

1. IDE → `文件 → 首选项 → 附加开发板管理器网址`，填：
   `https://dl.espressif.com/dl/package_esp32_index.json`
2. `工具 → 开发板 → 开发板管理器`，搜 `esp32`，装 **esp32 by Espressif Systems 3.3.11**
3. **装不动/报 Download failed** → 用仓库里的离线脚本：

```powershell
cd Epaper\_环境
.\download_esp32.ps1 -All
```

17 个文件 1.8 GB，带断点续传 + SHA256 校验，下到
`%LOCALAPPDATA%\Arduino15\staging\packages\`，IDE 会自动复用不再联网。
中途断了可以反复跑。

> 💡 如果装了 Steam++ / Watt Toolkit，大文件传输可能被它掐断 →
> 加 `-NoProxy` 绕开，或用 `start_arduino_no_http2.bat` 启动 IDE。

### 3.3 ⚠️ 板卡选项必须一字不差

打开任意 `.ino` 后，`工具` 菜单里设成：

```
开发板: ESP32S3 Dev Module
USB Mode: Hardware CDC and JTAG
USB CDC On Boot: Enabled
CPU Frequency: 240MHz (WiFi)
Flash Mode: QIO 80MHz
Flash Size: 4MB (32Mb)        ← ⚠️ 不是 8MB
PSRAM: OPI PSRAM              ← ⚠️ V2 必须选 OPI，不是 QSPI
Partition Scheme: Default 4MB with spiffs
Upload Speed: 921600
```

原电脑上这份配置的原始字符串（可直接对照）：

```
esp32:esp32:esp32s3:UploadSpeed=921600,USBMode=hwcdc,CDCOnBoot=cdc,
MSCOnBoot=default,DFUOnBoot=default,UploadMode=default,CPUFreq=240,
FlashMode=qio,FlashSize=4M,PartitionScheme=default,DebugLevel=none,
PSRAM=opi,LoopCore=1,EventsCore=1,EraseFlash=none,JTAGAdapter=default,ZigbeeMode=default
```

### 3.4 验证环境是否装好

打开 `02_Roadbook/02_Roadbook.ino` → 点「验证/编译」。

- **能编过** = 环境 OK
- 报 `GxEPD2_154_GDEY0154D67 not found` = GxEPD2 版本太旧（要 ≥1.6.x）
- 报 `Adafruit_GFX.h not found` = 漏装 Adafruit GFX Library

> ⏱️ **原电脑实测：完整编译一次约 41.6 分钟**。这台机器（i5-4300U / 3.9GB RAM）
> 是明确瓶颈，新电脑一般会快很多，但**别第一次就盯着进度条**。

---

## 4. 新电脑上 git 连不上 GitHub 怎么办

**症状**：`git push` 报 `schannel: next InitializeSecurityContext failed:
CRYPT_E_NO_REVOCATION_CHECK`。

**原因**（原电脑实测）：Steam++ / Watt Toolkit 把 github 域名劫持到 `127.0.0.1`
做本地反代加速，证书本身可信，但吊销列表（CRL）联不上外网 → 校验失败。

**解法**（一条命令，已加进原电脑的全局配置）：

```bash
git config --global http.sslBackend schannel
```

> ⚠️ 只设 `http.schannelCheckRevoke=false` **无效**，必须设 `sslBackend`（实测过）。
> 如果用 curl 手工调 GitHub API，要加 `--ssl-no-revoke`。

---

## 5. 项目结构（新电脑上你会看到什么）

```
Epaper/
├── README.md                    ← 项目总览（硬件/引脚/数据流/预览器用法）
├── 新电脑恢复指南.md              ← 你正在读的这篇
├── 00_HelloScreen/              ← 第 0 步：点屏（已验证里程碑，别改坏）
├── 01_Paging/                   ← 第 1 步：长文本分页
├── 02_Roadbook/                 ← 第 2 步：路书（当前活跃）
│   ├── 02_Roadbook.ino            固件：渲染模板 + 按键 + 电源 + RTC/电量页脚
│   ├── roadbook.h                 ⚠️ 自动生成，不要手改
│   ├── sample.json                路书数据源（人编辑这个）
│   └── preview/                   渲染预览图
├── tools/                       ← PC 侧工具
│   ├── roadbook.py                渲染核心（与固件共用同一套规则）
│   ├── preview_roadbook.py        路书预览器（零编译成本试排）
│   ├── preview_paging.py          长文本预览器
│   ├── roadbook_gen.py            JSON → roadbook.h
│   └── trim_gxepd2.py             GxEPD2 瘦身（省编译时间，可逆）
├── _archive/                    ← 旧 V1 工具链（SD 卡方案）
├── _环境/                       ← 离线装板卡包的脚本
├── 第0步/第1步/第2步_*.md        ← 三份中文教程
└── .workbuddy/memory/           ← 项目记忆：决策史 + 踩坑记录（换电脑续接靠它）
```

**改排版之前先跑预览器**（这台机器编译太慢，先在电脑上定稿）：

```bash
python tools/preview_roadbook.py --time 14:32 --batt 86
```

---

## 6. 继续开发的三条铁律（违反必出故障）

1. **`setup()` 第一行必须锁电池电**：
   `pinMode(17, OUTPUT); digitalWrite(17, HIGH);`
   漏掉 → 插 USB 一切正常，**电池模式一按松手就白屏**。
2. **`attachInterrupt` 之后立刻同步按键内部状态**（否则表现成「开机后马上关机」）。
3. **改渲染规则要改两处**：`tools/roadbook.py`（Python）和 `02_Roadbook.ino`（C）
   是手抄对齐的两份实现，只改一边预览器就会骗人。

详细版见 `.workbuddy/memory/MEMORY.md` 的「铁律」一节。

---

## 7. 以后怎么保持同步

原电脑上改完东西：

```bash
git add -A
git commit -m "说明改了什么"
git push
```

新电脑上开工前：

```bash
git pull
```

**别忘了 `.workbuddy/memory/` 会自动跟着走**（已从 .gitignore 里放行），
所以两台的「项目记忆」是同步的 —— 每次有重要决策让 AI 记一笔，就自动进版本库了。
