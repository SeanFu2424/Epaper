# 墨水屏骑行路书 — 新电脑恢复指南

> 这是项目从一台电脑迁移到另一台电脑时的快速恢复手册。
> 原电脑的位置：`C:\Users\surface\WorkBuddy\2026-09-07-15-27-20\`

---

## 1. 把这个项目放到新电脑

整个项目总共 **184 KB**（已经排除了构建产物），可以原样放在任意目录，例如：

- `C:\Users\surface\WorkBuddy\<新会话名>\`

**目录结构**（解压后应是这种样子）：

```
EinkRoadbook/
├── 00_HelloScreen/                       ← 当前要烧的 ESP32 固件
│   └── 00_HelloScreen.ino
├── _环境/                                 ← 离线下载板卡包的脚本
│   ├── download_esp32.ps1
│   └── start_arduino_no_http2.bat
├── _archive/                                ← 旧的 V1 完整工具链（568 KB）
│   ├── tools/render_roadbook.py           ← PC 端路书渲染器（Python+Pillow）
│   ├── tools/verify_bin.py
│   ├── firmware/EinkRoadbook_V1/*.ino     ← SD 卡翻页固件
│   ├── samples/roadbook_sample.json
│   ├── out/                               ← 已删除；需要时跑渲染器重新生成
│   ├── README_先看我.md
│   ├── 第1课_点亮屏幕并显示42.6km.md
│   ├── 墨水屏骑行路书_学习与工作清单.md
│   ├── 开发板硬件资源表_模板.md
│   ├── StemNotes_UI原型.html
│   └── 墨水屏路书_可执行清单.html
├── 第0步_先让屏幕亮起来.md                ← 当前唯一活跃的文档
├── 新电脑恢复指南.md                        ← 你正在读的这篇
└── .workbuddy/memory/                      ← 项目级记忆（不跨设备，下面有说明）
```

---

## 2. 哪些东西不会跟着账号走

WorkBuddy 的记忆分三层，处理方式不一样：

| 层 | 在哪儿 | 跨设备？ | 你需要做的 |
|---|---|---|---|
| 云端 Profile | WorkBuddy 服务端 | **自动同步** | 不用动 |
| 历史对话检索 | WorkBuddy 服务端 | **自动同步** | 不用动 |
| **项目级记忆** | `.workbuddy/memory/` | **不同步** | 已在包里，带走就行 |
| 用户级记忆 | `~/.workbuddy/MEMORY.md` | **不同步** | 这份内容只跟个人偏好有关，重新积累 |
| Arduino 板卡包 | `AppData\Local\Arduino15\staging\packages\` | 不同步 | 用包里 `_环境/download_esp32.ps1` 重下 |
| GxEPD2 库 | Arduino libraries 目录 | 不同步 | IDE 里重装 |

> 项目记忆我都在 `.workbuddy/memory/2026-09-07.md` 和 `2026-09-09.md` 里——你继续开发时这些会自动被读到。

---

## 3. 新电脑第一件事：装 ESP32 开发环境

按 **`第0步_先让屏幕亮起来.md`** 走，三步：

1. 装 Arduino IDE 2.x
2. 文件 → 首选项 → 附加开发板管理器网址，填：
   `https://dl.espressif.com/dl/package_esp32_index.json`
3. 工具 → 开发板 → 开发板管理器 → 搜 `esp32` → 装 **esp32 by Espressif Systems 3.3.11**

### 如果装板卡包时再次撞到 GitHub 下载问题

直接跑包里的脚本：

```powershell
cd C:\Users\surface\WorkBuddy\<新目录>\EinkRoadbook\_环境
.\download_esp32.ps1 -All
```

17 个文件 1.8 GB，下完放进 Arduino 缓存目录，IDE 会直接复用不再联网。
中途断了可以反复跑，**已完成文件会跳过，部分文件自动续传**。

如果脚本也连不上 GitHub（跟现在的电脑情况相同），按 `第0步_先让屏幕亮起来.md` 第 2 节里"装不上？报 Download failed"的诊断流程处理。

---

## 4. 新电脑第一件事（之二）：点屏验证

打开 `00_HelloScreen/00_HelloScreen.ino`，按 `第0步_先让屏幕亮起来.md` 第 4 节设置 Board 参数（V2 选 `PSRAM = OPI PSRAM`，**Flash Size 选 4MB(32Mb)**，**不要选 8MB**——我之前误写过 8MB，已纠正）。

上传代码，屏幕应显示 `42.6 km`，串口 115200 应打印 `boot / refreshed / done`。

---

## 5. 项目进度

| 阶段 | 状态 |
|---|---|
| 第 0 步：屏幕点亮 | **进行中** — 板卡包下载卡在 Steam++ 代理问题 |
| 第 1 课：点屏 + 显示 "42.6 km" | 文档已写（在 `_archive/`），等第 0 步通过 |
| 旧 V1 完整工具链 | 在 `_archive/`，渲染器/固件/示例路书 |
| V2：BLE + 设备端简渲染 | 待启动 |
| GPX → JSON 分析器 | 待启动 |

**下一步**：等 esp32 板卡包装上、屏幕点亮，再谈显示什么 / 怎么传内容（SD 卡还是 BLE）。

---

## 6. 容易踩的坑（已踩过，迁移时直接绕过）

1. **板子电源开关 GPIO6 必须拉低** — 否则屏幕白死无反应，这是这块板最高频翻车点
2. **屏幕 SPI 引脚** — 必须 `SPI.begin(12, -1, 13, -1)` 重映射（默认 MOSI=11 错位）
3. **V2 不是 8MB Flash** — V1/V2 唯一区别是 PSRAM 类型（V2=OPI），Flash 都是 4MB
4. **HTTP/2 长传被 Steam++ MITM 掐断** — 脚本强制 `--http1.1` 绕过
5. **板卡管理器下不动 GitHub** — 用 `_环境/download_esp32.ps1` 离线下载塞缓存

详细见 `第0步_先让屏幕亮起来.md` 的「踩过的坑」一节。

---

## 7. 同步与备份

以后任何时候要再迁电脑，重复这五步：

1. 把整个项目目录拷走（180 KB 量级）
2. 新电脑装 Arduino IDE + GxEPD2
3. 跑 `_环境/download_esp32.ps1` 重建板卡包缓存
4. 打开 `00_HelloScreen.ino`，按文档烧一次
5. 串口打印 `boot / refreshed / done` = 环境恢复成功