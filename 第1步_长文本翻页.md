# 第 1 步：长文本 → 自动分页 → 按键翻页

> 目标：证明"一屏放不下的内容能自动切成多页，按键能翻，边界不越界"。
> 这一步**不碰中文、不碰 SD 卡、不碰 deep sleep**，只验证翻页链路本身。

---

## 为什么先做翻页，而不是先重做显示内容

| | 先做翻页（**本步**） | 先做内容重构 |
|---|---|---|
| 本质 | 立**骨架** | 填**数据** |
| 骨架好了之后 | 内容随便换，不返工 | 翻页没通，内容做得再好也翻不动 |
| 风险 | 一次暴露 3 个真坑（见下） | 坑留到最后，返工成本最高 |

**翻页藏着 3 个必须先踩的坑，它们全都跟内容无关：**

1. **刷新方式** —— 全刷稳但慢（约 2 秒），局部刷快（约 0.5 秒）但有残影。
2. **翻页状态存在哪** —— 后面上 deep sleep 后普通变量会丢，要用 `RTC_DATA_ATTR`。
3. **按键判定** —— 短按/长按怎么分、消抖、**刷新那 2 秒里按的键会不会丢**。

**中文是第 4 个独立的坑**（点阵字库、字号、全角半角宽度、Flash 占用）。
如果一开始就混进来，出问题会分不清是"排版错"还是"翻页错"。所以拆开做。

---

## 一、要改东西，改哪里

**全部都在 `01_Paging\01_Paging.ino` 一个文件里**，没有第二个地方。

| 想改什么 | 改哪一行 | 现在的值 |
|---|---|---|
| **显示的内容**（路书正文） | 第 **104–153** 行 `SAMPLE_TEXT` | 千岛湖 128km 示例 |
| **页眉标题** | 第 **72** 行 `#define RB_TITLE` | `"QIANDDAO LAKE"` |
| **页脚提示** | 第 **73** 行 `#define RB_FOOTER` | `"PWR:next  BOOT:prev  HOLD:off"` |
| **正文 + 页眉字体** | 第 **49** 行 `#define BODY_FONT` | `FreeSans9pt7b` |
| **关机那个 OFF 大字** | 第 **50** 行 `#define BIG_FONT` | `FreeMonoBold18pt7b` |
| **行高** | 第 **80** 行 `#define LINE_H` | `16` |
| **每页几行** | 第 **81** 行 `#define LINES_PER_PAGE` | `9` |
| **左边距 / 正文宽 / 首行基线** | 第 77 / 78 / **79** 行 | `6` / `188` / `40` |
| **全刷还是局部刷** | 第 **92** 行 `FAST_PARTIAL` | `0`（全刷） |

> 行号是我刚把字体改成宏之后的位置。**改内容时行号会变**，以 `#define` 名字为准。

### 换字体只改一行（这是我刚帮你改好的）

以前换字体要动 **3 个地方**（`#include`、`setFont()`、量宽度的 `textWidth()`），漏一处就会宽度算错、行重叠。
现在全收进第 49、50 行两个宏了，`textWidth()` 里也用宏，**改一行全局生效**。

可用字体都在这个文件夹里（48 个）：

```
C:\Users\surface\Documents\Arduino\libraries\Adafruit_GFX_Library\Fonts\
```

常用的几个：

| 字体名 | 特点 |
|---|---|
| `FreeSans9pt7b` | **当前用的**。清晰，一页能放 8–9 行 |
| `FreeSansBold9pt7b` | 同样大小但更粗，**骑车时更好认**，代价是一页少放一点 |
| `FreeSans12pt7b` | 大一号，更清楚，但一页只能放 6–7 行 |
| `FreeMono9pt7b` | 等宽字体，数字对得齐（路书里里程、坡度多的话可以考虑） |
| `FreeSansBold12pt7b` | 又大又粗，适合只放几行的"大字模式" |

> ⚠️ **换更大/更粗的字体后，`LINE_H` 和 `LINES_PER_PAGE` 必须跟着改**，否则行会重叠、或压到页脚那条线。
> 不用你自己算——预览器会告诉你该调成多少（见下节）。

---

## 二、上传前先看效果：排版预览器

**这是这一步最重要的新增能力**——不用烧录就能看到屏幕长什么样。

```bash
cd C:\Users\surface\WorkBuddy\2026-09-07-15-27-20
python tools/preview_paging.py
```

输出在 `01_Paging/preview/`：

| 文件 | 是什么 |
|---|---|
| `page_01.png` … `page_12.png` | 每一页单独一张，放大 4 倍 |
| `page_off.png` | 关机时那个 `OFF` 画面 |
| `contact_sheet.png` | 全部页拼成一大张，一眼看完整本 |

它**不是"大概画一下"**，是像素级一致：

- 排版参数（边距、行高、每页行数、标题、页脚）**直接从 `.ino` 里读 `#define`**
- 正文**直接从 `.ino` 里抠 `SAMPLE_TEXT`**
- **字体从 `.ino` 的 `BODY_FONT` 读**（改一行，预览和固件一起换）
- 折行算法**1:1 照搬 C 那份**
- 每个字的像素**从 Arduino 库的字体头文件里读**，按 `Adafruit_GFX::drawChar` 的同一套规则画点

**改固件 → 预览跟着变，两边不可能对不上。**

### 先试再改：不动固件试字体 / 行高 / 每页行数

这三个参数只影响预览，**一个字都不会改你的 `.ino`**——所以可以随便试，试满意了再去改固件：

```bash
python tools/preview_paging.py --font FreeSans12pt7b          # 试大一号字
python tools/preview_paging.py --font FreeSansBold9pt7b       # 试更粗的字
python tools/preview_paging.py --font FreeSans12pt7b --line-h 22 --lpp 7
                                       # 字号 + 行高 + 每页行数 一起试
python tools/preview_paging.py --font Xxx --out 01_Paging/preview_try   # 输出到别的目录，不覆盖
```

试排时控制台会多打一行 `[试排模式：不会改固件]`，提醒你这不是最终配置。
**试好了，把那几个数填回 `.ino` 的 `BODY_FONT` / `LINE_H` / `LINES_PER_PAGE`，再烧一次。**

### 三个自动自检（不用靠眼睛看）

脚本跑完会打印这些，**任何一条报警都别烧录**：

```
layout: 105 lines -> 12 pages  maxw=188/188 over=0  chk=0xA461
!! 行 4 和行 5 在垂直方向重叠 1px（LINE_H=16 太小）
   这字体的自然行高是 22px，把 LINE_H 调到 22 以上，或减小每页行数
正文最低墨迹 y=172（页脚线 178，还剩 6px 空隙）
```

| 自检 | 正常 | 报警意味着 |
|---|---|---|
| `over=` | **必须 0** | 有行超出正文宽度，会横向溢出（老 bug 的报警器） |
| 行距自检 | `行距 OK：…还剩 Npx` | 报 `重叠 Npx` = 上下两行贴一起/压上了，**调大 `LINE_H`** |
| 正文最低墨迹 | `还剩 6px 以上` | 报 `快压到页脚线` = 正文吃进页脚，**减小 `LINES_PER_PAGE`** |

> 行距自检是这次新加的——**它就是之前"行重叠"那个坑的自动探测器**。
> 之前只能靠肉眼看图，现在数字直接告诉你哪两行撞了、撞了几像素。

### 📌 顺带查出来一件事：现在这套参数差 1px

上面那段报警**不是举例，就是当前正式配置的真实输出**：

| 配置 | 结果 |
|---|---|
| 现在：`9pt` + 行高 `16` + 每页 `9` 行 | 第 4/5 行**重叠 1px**，12 页 |
| 试改：行高 `18` + 每页 `8` 行 | 最紧处**还剩 1px 空隙**，14 页 |

1px 肉眼基本看不出来（而且是两个字符在水平方向未必对上的情况下），**所以不改也能用**。
但既然要改字体/内容，建议顺手把 `LINE_H` 改成 `18`、`LINES_PER_PAGE` 改成 `8`，一次编译到位。

想先看看效果再定：

```bash
python tools/preview_paging.py --line-h 18 --lpp 8
```

### 还能对账

脚本最后会打印这么一行：

```
layout: 105 lines -> 12 pages  maxw=188/188 over=0  chk=0xA461
```

烧录后串口 115200 会打印**一模一样的一行**。两行一致 = 电脑上看到的就是屏幕上显示的。

> `maxw` = 最宽那一行多少像素，`over` = 有几行超宽（**必须是 0**，不为 0 就会和下一行重叠）。
> 以前没有这个自检，超宽了要**眼睛**才发现，现在开机就报。

---

## 三、怎么用

**烧录**：打开 `01_Paging/01_Paging.ino`，板卡设置跟第 0 步一样（PSRAM = OPI PSRAM），点上传。

**按键**（全用板载按键，不用接线）：

| 按键 | 操作 | 效果 |
|---|---|---|
| **PWR** | 短按 | 下一页 |
| **PWR** | 按住 1.5 秒 | 关机（屏幕显示 `OFF`，然后断电） |
| **BOOT** | 短按 | 上一页 |

> 上电后**前 3 秒不响应按键**，这是故意的——防止把"按住 PWR 开机"误判成"长按关机"。

**开机串口应该看到**：

```
boot - paging test (reset reason 1)
layout: 105 lines -> 12 pages  maxw=188/188 over=0  chk=0xA461
page 1/12 (full)
ready - PWR=next  BOOT=prev  hold PWR=off
```

---

## 四、这次修的三个问题（成因都记下来，以后不再犯）

### 问题 1：有些行太长，换行后和下一行重叠

**根因**：量宽度用的字体不对。

`buildLines()` 跑在 `setFont()` **之前**，而 `display.getTextBounds()` 量的是"当前字体"。
所以量出来的是**内置 5×7 字体**的宽度（每字符约 6px），而真正画出来的是 **FreeSans9pt7b**（小写字母约 10px）——
宽度被低估了近一半。于是"以为塞得下"，实际溢出，Adafruit_GFX 就自动换行；
换行后用的行距是字体的 `yAdvance`（22px），不是我们的 `LINE_H`（16px），**正好压到下一行上**。

**修法**（三保险）：

1. 新增 `textWidth()`，直接读字体表里的 `xAdvance` 算宽度。**和屏幕上画出来的宽度完全一致，跟当前字体无关。**
2. `buildLines()` 之前先 `setFont(&FreeSans9pt7b)`
3. `display.setTextWrap(false)` —— 万一还有超宽行，宁可裁掉也不要折下去压行

顺带把 `BODY_MAX_W` 从 186 修正到 **188**（= 200 − 左右边距各 6）。

### 问题 2：翻页后要等 2 秒，快速按无效

**根因**：墨水屏全刷要 2 秒，这 2 秒里 `loop()` 卡在 `display.nextPage()` 里，
**轮询根本读不到按键**。你在刷新期间按的那几下，程序压根没看见。

**修法**：PWR / BOOT 都挂 **GPIO 中断**，按下和松开各记一个时间戳（含 30ms 去抖）。
刷新一结束，`loop()` 立刻消费这些事件 —— **这 2 秒里按的键一个都不会丢**。

### 问题 3：编译花了将近 1 小时

见下一节——**这不是代码问题，是这台电脑的客观条件 + 缓存没保住**。

---

## 五、编译慢怎么办

> 这一节是**实测**出来的结论（我在你这台机器上用命令行完整编了一遍），
> 和"看起来应该很快"的直觉不一样，值得认真看一遍。

### 实测数据

| 事实 | 数据 |
|---|---|
| 完整编一遍 `01_Paging` | **41.6 分钟**（编过了，Flash 27%，RAM 13%） |
| 紧接着**什么都不改**再编一遍 | 超过 **17 分钟**还在跑，而且**构建目录里的 .o 全被删了** |
| GxEPD2 库里有多少个 `.cpp` | **104 个**（我们只用其中 **1 个**） |
| ESP32 核心有多少个源文件 | 59 个 |

**结论一：这台机器上，"每次编译 30~60 分钟"就是基线。**
i5-4300U 是 2013 年的 2 核低压 U，内存只有 3.9 GB。这个工作量在它身上就是这个速度。

**结论二：不要指望"第二次会变快"。**
实测发现每次编译都会**把构建目录清空从头编**——我特意把上一轮编好的核心对象复制进去过，
下一轮照样被清掉。所以"有缓存就会秒编"在这里不成立。

**结论三：唯一真正有效的办法是"让编译器少编点东西"。**

### 措施 ① 给 GxEPD2 瘦身（**效果最大**）

GxEPD2 支持上百款墨水屏，`src/` 下有 **104 个 `.cpp`**。
Arduino 编库时会把它们**全部**编一遍，不管你用不用。你这块板只用 `GDEY0154D67` 一个，
剩下 102 个（4.2 寸、6 寸、7.8 寸、10.3 寸…）纯属陪跑。
更亏的是每次编译还要对它们跑两遍（一遍依赖探测、一遍真编译）。

**已经写好脚本**，默认只空跑不动任何东西：

```bash
cd C:\Users\surface\WorkBuddy\2026-09-07-15-27-20

python tools/trim_gxepd2.py            # 空跑，只列清单（已跑过：102 个文件 1.7 MB）
python tools/trim_gxepd2.py --apply    # 确认后执行
```

`--apply` 之后**重新编译一次**：能编过 + 屏幕显示正常，以后就一直这么快。
万一报错：

```bash
python tools/trim_gxepd2.py --restore  # 一条命令完全还原，不留痕迹
```

安全性核对过（是"挪走"不是"删除"）：

- 没有任何 `.h` 文件 `#include` 了 `.cpp`
- 要留下的两个文件（`GxEPD2_EPD.cpp` + `GxEPD2_154_GDEY0154D67.cpp`）只引用自己和基类
- 基类实现只在 `GxEPD2_EPD.cpp` 里

> **注意**：我**没有替你执行**这一步——它会动到 `Documents\Arduino\libraries\GxEPD2`
> 里的文件，这种改动需要你点头。等你有 20 分钟不用板子的时候跑一次就行。
> 预计能把编译时间砍掉一大半（估算降到 15~20 分钟量级，以实测为准）。

### 措施 ② 给杀软加排除目录

Windows Defender 的实时扫描会**逐个检查**编译产生的每个 `.o` / `.d` 文件（几千个）。
在这台 2 核机器上这一项占掉的时间不少。

**用「管理员身份」打开 PowerShell**，粘贴这四行：

```powershell
Add-MpPreference -ExclusionPath "C:\Users\surface\AppData\Local\Arduino15"
Add-MpPreference -ExclusionPath "C:\Users\surface\AppData\Local\arduino"
Add-MpPreference -ExclusionPath "C:\Users\surface\Documents\Arduino"
Add-MpPreference -ExclusionPath "C:\Users\surface\WorkBuddy\2026-09-07-15-27-20"
```

验证：

```powershell
Get-MpPreference | Select-Object -ExpandProperty ExclusionPath
```

> 这四行只是"别扫这些文件夹"，不影响安全性——里面全是编译产物和你自己的代码。

### 措施 ③ 少编几次（用预览器代替"烧了再看"）

既然一次编译要几十分钟，**就别靠烧录来试排版**。

先用预览器把版面定好，一次改到位，再烧一次。**试字体/行高/每页行数用试排模式，完全不用编译**：

```bash
python tools/preview_paging.py --font FreeSans12pt7b --line-h 22 --lpp 7
```

三个自检全绿了（`over=0`、行距 OK、没压页脚），再把那几个数填回 `.ino` 烧一次。

### 措施 ④ 别动 Tools 设置、别清理这几个目录

| 目录 | 是什么 |
|---|---|
| `C:\Users\surface\AppData\Local\arduino` | 编译缓存 |
| `C:\Users\surface\AppData\Local\Arduino15` | 板卡包、工具链 |
| `C:\Users\surface\Documents\Arduino` | 库 |

机器上有个 `SysCleanProX` 之类的清理工具，**别让它碰这三个目录**。

板卡设置也**保持现状别动**（已从 IDE 的构建记录里读出真值）：

| 设置项 | 值 |
|---|---|
| Board | ESP32S3 Dev Module |
| USB CDC On Boot | Enabled |
| PSRAM | **OPI PSRAM** |
| Flash Size | 4MB (32Mb) |
| Flash Mode | QIO 80MHz |
| Partition Scheme | **Default 4MB with spiffs**（默认那个） |
| CPU Frequency | 240MHz |

> 之前文档里写过"分区选 Huge APP"，那是**错的**——实际就是默认的 `default`。别改。

### 编译时顺手别做的事

- 别同时开串口监视器（会和上传抢 COM 口）
- 内存只有 3.9 GB，编译时把浏览器/其他大程序关掉
- 别再新建一个"复制一份改名"的 sketch 文件夹——**新文件夹 = 新缓存键 = 大概率全量重编**

---

## 六、要观察的点（请把数字记下来）

| # | 观察什么 | 怎么测 | 记录 |
|---|---|---|---|
| 1 | **全刷一页要多久** | 按一下 PWR，心里数秒（约 2 秒） | ______ 秒 |
| 2 | **有没有残影** | 翻 5 页后回看，上一页的字有没有淡影 | 有 / 无 |
| 3 | **边界对不对** | 在第 1 页按 BOOT、在最后一页按 PWR | 正常 / 异常 |
| 4 | **快速连按还会丢吗** | 翻页瞬间连按 3 下 | 全丢 / 丢部分 / 都跟上了 |

第 1、2 两个数字用来决定：**骑车时用全刷还是局部刷**。

### 想更快？翻页有个开关

`01_Paging.ino` 里这一行：

```c
#define FAST_PARTIAL     0     // 0 = 全刷(约2秒，干净)   1 = 局部刷(约0.5秒，有残影)
```

改成 `1` 再上传，翻页能快约 4 倍，代价是**会累积残影**。
先用 `0` 把正确性验证完，再回头试 `1`。

> 这个面板 GxEPD2 驱动里写着 `hasPartialUpdate = true`，局部刷是**支持**的
> （`partial_refresh_time = 500ms` vs `full_refresh_time = 2000ms`），代码路径已经写好。
> 但"支持"和"残影能不能忍"是两件事，得眼睛看。

---

## 七、代码里已经做的判断（供你对照）

- 长文本写在固件里（`SAMPLE_TEXT`），**不用 SD 卡** —— 少一个变量
- `SAMPLE_TEXT` 里**不要手动折行**，一行写到底，交给代码按宽度折
  （手动折 + 自动折 = 折两次，会冒出 `points`、`km` 这种孤字）
- 折行在**开机时算一次**（`buildLines()`），翻页时直接取行，所以翻页本身很快
- 页码从 1 开始显示（内部 0 起算）
- 排版参数：每页 9 行、行高 16px、正文宽 188px（预览器确认 `over=0`）
- 页脚用 5×7 内置字体（FreeSans 9pt 太宽，脚注会出屏）

---

## 八、已知的"故意不做"

| 没做 | 什么时候做 |
|---|---|
| 中文显示 | 第 2 步（现成方案：U8g2 的文泉驿点阵字库 `wqy12_t_gb2312`） |
| 从 SD 卡读内容 | 第 3 步（`_archive` 里的 V1 固件已有读 bin 的代码） |
| deep sleep 省电 | 第 4 步（官方 `11_RTC_Sleep_Test`；注意 GPIO17 要 `gpio_hold_en`，否则电池模式下等于断电） |
| 事件不被分页切断 | 待定 —— 现在是一路顺排，所以出现过 `KM 8.6 FEED 1` 在页尾、说明在次页的情况。要做"一个事件尽量不跨页"得改成按块分页，**先看预览决定要不要** |

---

## 九、下一步

翻页骨架稳了之后，再谈"路书卡片长什么样"——那时候才有意义。

到时候有两个方向可选，都用预览器先看再烧：

1. **不跨页分页**：让每个 `KM x.x` 事件尽量整块留在一页里
2. **中文**：PC 端渲染整页位图（`_archive/tools/` 里那套已能跑），或设备端点阵字库
