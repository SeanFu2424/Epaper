# 第 2 步：Roadbook V1 —— 7 种事件的电子路书

> 目标：把 01_Paging 的"固定长文档"换成"路书事件清单"，
> 每一页只显示"哪些公里有什么事件"，KM 左对齐 + 事件右对齐，
> 爬坡占两行，自制箭头/星级位图代替字体里没有的符号。
>
> **数据与渲染分离**：改路线改 JSON（不重编 .ino 的渲染部分），换模板改 .ino。
>
> **V1.8（2026-09-18）**：坡内提醒（`sub=1` 的 GLU）**夹在爬坡两行中间**显示，
> 保证块内公里数严格递增（以前排在爬坡块后面，屏幕上会出现 34.2 → 48.6 → 43.4 的倒挂）。
> 分页单位从"事件"改成"块"（爬坡块 = 首行 + 坡内提醒 + 末行，整块不跨页）。
>
> **V1.7（2026-09-18）**：新增 `cp` 事件（GPX 航点里的固定补给点，显示 CP1..CPn）；
> 同一公里数的事件合并成一行（如 `48.7 km  CP2 ↙`）；转弯只保留 5 条大弯。
>
> **V1.5（2026-09-11 第二轮）**：爬坡第二行的右列从 `+237m 6.1%` 改成 **坡度% + 难度星级**；
> 新增 `halfway` 中点事件；**去掉补水（H2O）**，补给统一显示 `GLU`。

---

## 一、V1 的事件类型（7 种）

| type | 显示 | 占几行 | 需要的额外字段 |
|---|---|---|---|
| `turn` | **公里数 + 方向箭头**（不显示文字） | 1 | `dir`: up / up_right / right / down_right / down / down_left / left / up_left（或简写 `u ur r dr d dl l ul`） |
| `glu` | 公里数 + **GLU** | 1 | 可选 `sub: 1` = 落在爬坡块内部（左列缩进 8px，夹在爬坡两行中间） |
| `climb` | 第一行：公里数 + **CLM 长度**<br>末行：**结束公里数**（缩进）+ **坡度% + 星级** | **2** | `length` (km)、`grade` (%)，可选 `stars` 1~5（不写就按 `grade`+`length` 自动算） |
| `cp` | 公里数 + **CP1 / CP2 …** | 1 | `label` 或 `n`（序号），来自 GPX 航点 |
| `danger` | 公里数 + **DANGER** | 1 | 无 |
| `finish` | 公里数 + **FINISH** | 1 | 无 |
| `halfway` | 公里数 + **HALFWAY** | 1 | 无 |

任何事件都可以再带一个 `arrow` 字段（方向名）—— 那是"同一个公里数既要补给又要拐弯"
被合并成一行时留下的，屏幕显示成 `48.7 km  CP2 ↙`。

**`glu` 和 `cp` 是两件事，不是二选一**：

- `cp` = GPX 航点里骑手**人工标的**固定补给点（有人有店，必须停），一条路线几个就是几个
- `glu` = 按骑行时间推算的**"该吃胶了"提醒**（默认每 40 分钟一个，不一定有店）

`climb` 是**唯一占两行**的事件，而且是一个"块"：首行 → 坡内提醒 → 末行，整块不跨页。

### 爬坡星级怎么定（规则抄 dincalculator 官网）

| 星级 | 条件 |
|---|---|
| ★ | 缓，3-4% |
| ★★ | 一般 |
| ★★★ | 扎实：≥5% 且 ≥2 km |
| ★★★★ | 难：≥7% 且 ≥3 km |
| ★★★★★ | 残酷：≥9% **或** ≥7 km |

**为什么不显示爬升/坡度**：200px 宽放不下（9pt 下 `+237m 6.1%` 会挤掉左列），
而且骑的时候"这段有多难"比"平均 6.1%"更直观。爬升数据仍留在 JSON/C 数组里，只是不上屏。

**为什么星级是位图**：`★` 在 U+2605，字体只覆盖 ASCII 0x20-0x7E，画出来是空白 ——
和箭头一样用程序画成 1-bit 位图（`tools/roadbook.py` 的 `make_star()`）。
⚠️ 位图**每行必须补齐到整字节**：`drawBitmap` 按 `ceil(width/8)` 字节取行，
16px 的箭头正好是 8 的倍数所以没事，13px 的星星不补行就会整体错位。

---

## 二、JSON 数据结构（V1 schema）

```json
{
  "name": "NANJING LOOP",
  "events": [
    { "km": 8.4,   "type": "turn",  "dir": "left"  },
    { "km": 15.2,  "type": "glu"                    },
    { "km": 22.6,  "type": "climb", "length": 3.1, "grade": 5.4 },
    { "km": 31.0,  "type": "danger"                 },
    { "km": 60.2,  "type": "halfway"                },
    { "km": 120.5, "type": "finish"                 }
  ]
}
```

`stars` 可以不写（渲染时按 `grade` + `length` 自动算）。存成 `02_Roadbook/sample.json`
（或任意路径）。**改路线就改这一个文件**。

> 完整示例见 `02_Roadbook/sample.json`；真实路线示例见 `02_Roadbook/fuzhishan.json`
> （由 `tools/gpx_to_roadbook.py` 从 GPX 自动生成）。

---

## 三、改完路线，怎么进设备（4 步）

```bash
# 0.（可选）从 GPX 自动生成路书 —— 爬坡/转弯/补给/中点全自动
python tools/gpx_to_roadbook.py "在覆卮山燃烧.gpx" --name FUZHISHAN --out 02_Roadbook/fuzhishan.json

# 1. 改 JSON（手改）
notepad 02_Roadbook/sample.json

# 2. 电脑上看效果（不用烧录）
python tools/preview_roadbook.py 02_Roadbook/fuzhishan.json --time 08:30 --batt 76%

# 3. 生成 C 头文件
python tools/roadbook_gen.py 02_Roadbook/fuzhishan.json
#   产生 02_Roadbook/roadbook.h  ← 固件 include 的就是它

# 4. Arduino IDE 打开 02_Roadbook.ino，上传
```

预览器会输出：
- `02_Roadbook/preview/page_01.png ... page_NN.png` 每页一张
- `02_Roadbook/preview/contact_sheet.png` 拼版图
- `02_Roadbook/preview/arrows.png` 8 个方向箭头图例
- `02_Roadbook/preview/stars.png` 星级位图例

控制台会打"自检"：**左右列没撞 / 没压页脚线**才算定稿。

---

## 四、屏幕参数（V1.4，实测后修订）

| 参数 | 值 | 含义 |
|---|---|---|
| 屏幕 | 200 × 200 | GDEY0154D67 |
| 字体 | **FreeSansBold9pt7b** | 9pt 加粗（骑行更易读），yAdvance=22 |
| 行距 | **自适应 24~38px** | 按每页行数自动撑开 + 垂直居中 |
| 每页最多 | **6 行** | 爬坡算 2 行 |
| 页眉 | y=16 基线，y=22 横线 | "路书名 1/N" |
| 正文区 | 基线 38~**162** | 实测正文墨迹底 = 基线 +1px |
| 左右边距 | 6 px | |
| **页脚横线** | **y=174** | V1.4 新增 |
| **状态栏** | **基线 y=190** | V1.4 新增：左时间 / 右电量 |

> **为什么不用 12pt 大字**：实测 "108.9 km" + "DANGER" = 196px > 可用 188px，
> 两列布局下 12pt 必超宽。9pt Bold 是这个屏的两列布局极限。
> （2026-09-11 试过 12pt + 4 行/页的版式，用户看过预览图认为不如现在好看，已回退。）

> **正文区为什么从 190 收到 162**：给页脚状态栏让出 28px。代价是 6 行页的行距
> 从 30 收紧到 24（字体 yAdvance=22，还剩 2px 余量；9pt 字形墨高仅 11px，
> 行间白 13px，视觉不挤）。实测三页距页脚横线分别 13/11/16px。

> **自适应行距的坑**：所有行必须等距（包括爬坡末行、坡内提醒）。一旦爬坡块里某两行
> "挤紧一点"，实际占用高度就和排版假设对不上，底部又会空出一块——这个坑刚踩过。
> 爬坡块内的归属感靠"缩进 8px"来体现，不靠紧挨。

**爬坡块**（v1.8）：首行左列 = 起点公里数 + `CLM 长度`；末行左列缩进显示
**结束公里数**（= 起点 km + CLM 长度，固件里定点整数相加 `km10 + length10`，零浮点），
右列 = 坡度% + 星级。**坡内提醒夹在两者中间**，公里数严格递增：

```
34.2 km            CLM 14.4
  43.4 km             GLU      ← sub=1，缩进，表示"在这块坡中间吃"
  48.6       5.5% ★★★★★      ← 末行：不带 " km" 单位（首行已给）
```

> 末行为什么省略 `" km"`：三位数公里数（`136.2 km` = 72px）+ 坡度 `5.5%`（40px）
> + 5 颗星（65px）= 181px，而可用宽度只有 188 − 8（缩进）= 180px，差 1px 就粘连。
> 省略单位省 20px，且**不损失数字精度**。
>
> ⚠️ 结束公里数是 `起点 + 长度` 反算的，所以生成器必须"先把终点取整、再用终点减起点
> 反推长度"，两边各自四舍五入会差 0.1 km（Far02 上真踩到两处）。

### 页脚状态栏（V1.4 新增）

```
┌────────────────────┐
│ NANJING LOOP   1/3 │  页眉 y=16 / 横线 y=22
│ ────────────────── │
│ 22.6 km      CLM 3.1│  正文 38..162
│   25.7 km  +180m 5.4%│
│ ...                │
│ ────────────────── │  ← 页脚横线 y=174（与页眉横线呼应）
│ 14:32          87% │  状态栏 y=190（左时间 / 右电量）
└────────────────────┘
```

**数据来源（全部来自官方源码/wiki 核对，不是推断）**：

| 显示 | 来源 | 硬件 |
|---|---|---|
| 左：`HH:MM` | 板载 RTC 芯片 **PCF85063**，I2C 地址 `0x51` | SDA=GPIO47，SCL=GPIO48 |
| 右：`NN%` | **VBAT = VADC × 2**（200K/200K 分压） | GPIO4 = ADC1 通道 3 |

- 时间读的是 PCF85063 寄存器 `0x04` 起连续 7 字节（秒/分/时/日/星期/月/年，BCD 编码）
- 秒寄存器 **bit7 = OS 标志**，为 1 表示芯片掉过电、时间不可信
- 电量百分比曲线（3.3V=0% … 4.15V=100%）**是按常见单节锂电特性拟的，不是官方值**，实测后可调

**读不到怎么办**：显示占位符 `--:--` / `--%`，**绝不影响路书正文渲染**。

### 怎么对时（RTC 首次使用 / 掉电后）

开机时固件会自动判断：**时间无效（OS 标志=1，或年份 < 2024）就用「编译时刻」写入**。
但编译到烧录有 40 分钟时差，所以接电脑时建议手动对一次：

打开串口监控（115200），发一行**回车结尾**：

```
T2026-09-11 11:35:00
```

成功会回显 `rtc set: 2026-09-11 11:35:00`，并立刻重画当前页。

> **需要实测才知道的事**：PCF85063 若没有后备电池/超级电容，断电后时间会丢，
> 每次开机都要重新对时（或退回编译时刻）。烧录后看串口这行的区别：
> - `rtc: ... (kept)` → 时间保住了，属于正常
> - `rtc: invalid (lost=1 ...) -> set compile time ...` → 掉过电，需要重新对时

### 刷新模式（V1.2：改回全刷）

| 宏 | 当前值 | 说明 |
|---|---|---|
| `FAST_PARTIAL` | **`0`** | 实测局部刷新残影太重，**改回 2 秒全刷**（V1.1 试过 0.5s 局部刷，用户实测不能忍） |
| `FULL_EVERY` | `4` | 仅 FAST_PARTIAL=1 时生效，现在无效 |

> 想再试局部刷新：`FAST_PARTIAL` 改 `1`。残影是墨水屏物理特性，定期全刷只能压制不能消除。

---

## 五、按键与开关机（V1.3）

| 按键 | 操作 | 效果 |
|---|---|---|
| PWR | **按住约 1.8 秒** | 开机（短按会被主动断电，屏幕毫无变化，看起来"没反应"） |
| PWR | 按下 | 前进（连按 N 次 = 直接翻 N 页，一次刷屏完成） |
| PWR | 按住 1.5 秒 | 关机（屏幕 `OFF` → 断电） |
| BOOT | **按下** | 后退 N 页 |

### 长按开机（V1.3 定稿）

**先说结论：官方例程里根本没有"软件长按开机"**——查 `07_BATT_PWR_Test` 源码，
`button_bsp.c` 里 `#define button2_active 0`（PWR 键有效电平 LOW），软件只管**长按关机**；
"开机要按久一点"是硬件按下供电 + 固件锁电之间的时间差造成的。
我们的固件 setup 只要 0.4 秒，所以**必须自己主动断电**才能做出"长按才开机"。

```
上电 → 立刻锁电(17=HIGH，保证程序活着)
     → ① 起手 300ms 内有没有人按？
         没有        → 当作 USB 插线上电，直接放行
         有          → ② 要求一直按到 BOOT_HOLD_MS(1000ms)
                        中途松手 = 短按 → 主动断电(17=LOW)，屏幕内容一点不变
                        按满窗口 = 长按 → 正常开机
```

- **为什么必须先锁电**：锁电前的任何延迟都会踩"电池模式一按松手就白屏"那个老坑
  （01 步试过 delay 2s/3s 全部失败）。先锁电 = 程序一定活着，主动权在固件手里。
- **USB 只多等 0.3 秒**；USB 下误短按也不会卡死（断电写不进去，等 2 秒继续启动）。
- 代价：电池开机从上电起要按约 1.8 秒。宏 `HOLD_TO_BOOT` 改 `0` 可关掉。

### 🔴 V1.2 的两个 bug（用户实测"短按才开机、长按开机又关机"）

**Bug A：短按反而能开机** —— 旧代码有前置条件：

```cpp
if (digitalRead(PWR_BTN) == LOW) {   // ← 祸根
    ...等 1.5 秒...
}
```

短按时 setup 跑到这里用户**早已松手** → 读到 HIGH → 整段被跳过 → 直接开机。
**教训：判断"用户有没有按键"不能只看某一瞬间的电平，必须采样一段时间。**

**Bug B：长按开机后马上又关机** —— `attachInterrupt()` 挂上时手还按着，
中断内部状态却是"未按下"。之后那次松手不会被记成 `pwrSawRelease`，
而长按关机只看 `pwrIsDown + 按够 1.5 秒`，于是刚开机就被判成"长按关机"。

修法两处：
1. attach 之后**立刻同步**内部状态：`pwrIsDown = 实际电平`、`pwrSawRelease = !pwrIsDown`
2. 长按关机加一道门：`if (pwrSawRelease && pwrIsDown && ...)` —— **必须先见过一次松手**

**V1 → V1.1 修的三个按键 bug**（都来自"快按无效"的实测）：

1. **原来必须等松手**：松开边沿若落在 30ms 去抖窗口内被整个吞掉 → 快按完全没反应。
   现在**按下即生效**（骑行时单手盲按，按下那刻就该有反馈）。
2. **刷屏 2 秒里连按只算一次**：旧代码用单槽标志，后按的覆盖先按的。
   现在用计数器（最多记 3 次），刷屏结束后合并成"翻 N 页"。
3. **开机 3 秒内按键全部丢弃**：开机保护 3000ms 太长，砍到 600ms。

> 代价：长按关机时，按下那一下已经翻过页了——关机画面会盖上去，无感。

---

## 六、4 个踩过的坑（再换字体/加事件类型时绕开）

### 1. 字体里没有箭头

FreeSans9pt7b 只覆盖 0x20-0x7E，↰ ↱ ← → ↙ ↘ 全部 U+2190 以上，**画出来是空白**。
V1 用数学方法生成 16×16 的 1-bit 位图（`tools/roadbook.py` 里的 `_in_arrow`），
PC 端和 ESP32 端用同一份数据（生成器 `roadbook_gen.py` 导出成 C 数组）。

### 2. 非等宽字体下，手打空格做右对齐是假的

V1 渲染器必须**测宽右对齐**：用 `display.getTextBounds()` 量出右列宽度，
再 `setCursor(EPD_W - MARGIN - bw - bx)` 把光标移到右边距处。
预览器自检会报警"左右列只差 Xpx"——X 必须 > 4 才安全。

### 3. 爬坡块不能跨页

`buildPages()` 用贪心：累加行数，超 `MAX_ROWS=6` 就换页；
但**分页单位是"块"不是事件**（v1.8 改）：

| 块 | 占几行 |
|---|---|
| 普通事件（turn / glu / cp / danger / finish / halfway） | 1 |
| 爬坡块 | 2 + 坡内提醒个数（首行 + 各条坡内 GLU + 末行），**整块不拆页** |

所以一条路线页数必须按"块"数算，不能拿行数直接除 6。
`tools/gpx_to_roadbook.py` 生成完会用 `check_row_order()` 摊开所有行做自检 ——
**任何一页出现公里数倒挂就直接报警**，别烧。

### 4. ESP32 PROGMEM 在 ESP32 上是空宏

`static const uint8_t RB_ARROWS[8][32] PROGMEM = {...}` 在 ESP32 上就是普通 Flash 数组。
读的时候 `pgm_read_byte(&RB_EVENTS[i].type)` 也会被编译器优化成直接访问。
所以不要在 ESP32 上用 `memcpy_P` 之类的 AVR 专用函数。

---

## 七、文件清单

```
02_Roadbook/
├── 02_Roadbook.ino       ← 渲染器（这一份是渲染逻辑，路线无关）
├── roadbook.h            ← 由 tools/roadbook_gen.py 生成
├── sample.json           ← 示例路书（你的路线从这里开始改）
└── preview/              ← 预览器输出（每改一次 sample.json 跑一次）
    ├── page_01.png
    ├── page_02.png
    ├── contact_sheet.png
    └── arrows.png
```

```
tools/
├── roadbook.py           ← 渲染核心（箭头位图 + 布局 + 分页）—— 零依赖
├── preview_roadbook.py   ← 电脑端预览器
└── roadbook_gen.py       ← JSON → roadbook.h（C 数组）
```

---

## 八、为什么先不上 JSON 解析

V2 会做：用 SD 卡读 JSON（数据结构在 V0.2 完全不变），
固件不再 include `roadbook.h`，改用 `SD.open("/roadbook.json")`。
**渲染器（`02_Roadbook.ino`）一行业不用改**——这就是数据/渲染分离的价值。

V1 不上 JSON 是因为：
- ArduinoJson 多编 30~60 秒 + 多占 Flash
- V1 的目标是"先把模板和渲染器在屏幕上定稿"，数据在哪不重要
- V0.2 加 SD 卡时再切换数据源
